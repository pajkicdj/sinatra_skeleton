<!-- concention to call a list page an index page, it's an index of all the messages-->

<% @messages.each do |message| %>
  <div class="message">
    <h3><a href="/messages/<%= message.id %>"><%= message.title %> <small>(by: <%= message.author %>)</small></h3>
    <p>
      <%= message.content %>
    </p>
  </div>
<% end %>

<a href="/">Back to Home</a>
<!-- erb works with aligator tags < % where values use = with AGT
and functionality like loops don't. we are dynamically making html here
by embedding ruby in the code-->